apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: metrics-app
  labels:
    release: prometheus
spec:
  selector:
    matchLabels:
      app: metrics-app
  endpoints:
    - port: http
      path: /metrics
      interval: 10s